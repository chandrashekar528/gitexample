pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code pulled from Git successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage running'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage running'
            }
        }
    }
}
